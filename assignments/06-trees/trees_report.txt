======================================================================
  GRADING REPORT
  Assignment 6 — Tree-Based Methods — CART, Random Forests, and Gradient Boosting (real estate)
  Student:
  Generated: 2026-06-17 10:37
======================================================================

1. SUBMISSION
----------------------------------------------------------------------
   All required files submitted successfully:
   - Results JSON: OK
   - Code File: OK
   - Slides (PDF): OK
   - Slides Code (generates the PDF): OK
   - Analysis Summary (free text, max 300 words): OK

2. YOUR PERSONALIZED DATA GENERATION PROCESS
----------------------------------------------------------------------
   The true model used to generate your data is revealed below
   so you can compare your submitted model to it and understand
   how your score was calculated.

   Tree-Based Methods: CART (from scratch, depth=3) + Random Forest + Gradient Boosting
   Data source:            Ames Housing (Kaggle, De Cock 2011) — real features, synthetic outcomes
   Training observations:  300
   Test observations:      200
   Predictors (p):         12
   Active vars (reg):      GrLivArea, OverallQual, YearBuilt, TotalBsmtSF, GarageArea
   Active vars (clf):      GrLivArea, OverallCond, YearBuilt, Fireplaces
   Regression noise sigma: 30.0
   Train alert rate:       0.1567
   Per-student reference targets (RF/GBM with fixed hyperparameters):
     RF  regression test MSE: 1300.431572
     GBM regression test MSE: 1227.971825
     RF  classification CE:   0.422897
     GBM classification CE:   0.423986
   Null test MSE:          11682.5063
   Null classifier log-loss: 0.405158

3. ASSIGNMENT ASSESSMENT
----------------------------------------------------------------------

3.1 PERFORMANCE OF YOUR INFERENCE/PREDICTIONS (weight 70%)
    ----------------------------------------------------------

   Scoring method: Negative Log-Likelihood (NLL)
   For each test observation i:
     NLL_i = 0.5 * log(2*pi*sigma_i^2) + (y_i - mu_i)^2 / (2*sigma_i^2)
   where mu_i = your predicted_mean, sigma_i = your predicted_sd,
   y_i = true test value.
   Your loss = average NLL over all test observations.

   Grade mapping:
     grade = 100 * (NLL_null - NLL_yours) / (NLL_null - NLL_oracle)
     clamped to [0, 100]
   Ceiling: if your predictions closely match the true active
   predictors (R² >= 0.98 vs oracle predictions), the ceiling
   is 90 — otherwise 95. Scores at or above the ceiling are
   rounded up to 100 to account for irreducible estimation
   error from finite sample size.
   where:
     NLL_oracle = the best possible NLL, achieved by a model that knows
                  the true coefficients and the true noise level.
     NLL_null   = the NLL of a naive model that simply predicts the
                  training mean (y_bar) with sd = sd(y_train) for every
                  test observation (i.e., ignores all predictors).
   A score of 100 means you matched the oracle; 0 means you did no
   better than ignoring the predictors entirely.

   No scoring data available.

3.2 PRESENTATION SLIDES (weight 20%)
    ----------------------------------------------------------

   Slides file submitted: YES
   Slides score: 100/100

3.3 CODE SUBMISSION (weight 10%)
    ----------------------------------------------------------

   Code file submitted: YES
   Code score: 100/100

3.4 AI FEEDBACK (on your analysis summary)
    ----------------------------------------------------------

   Note: This feedback was generated by an AI assistant (LLM).
   It may contain inaccuracies. Use it as a learning aid, not
   as a definitive assessment. Your grade is determined solely
   by the deterministic scoring above.

   Your summary describes a scratch CART regressor that uses greedy
   midpoint splits maximizing SSE reduction and predicts the node
   mean in each leaf. You report a shuffled five-fold CV sweep over
   depths 2 through 10 that selected depth 6 with MSE 2311.325, and
   you note that your submitted depth-3 tree (min_samples_leaf=5)
   matches the permitted sklearn depth-3 tree to numerical precision
   on the 200 test predictions.
   For the regression ensembles, you report a scratch random forest
   of 2,000 CART trees with bootstrap samples of 300 rows, mtry=4,
   max_depth=8, and min_samples_leaf=1, with repeated-CV MSE
   1484.850 (RMSE 38.534) and importance computed as normalized
   equal-tree Mean Decrease in Impurity. Your XGBoost regression
   configuration is described as 386 trees with learning_rate=0.05,
   max_depth=1, and 0.85 row/feature subsampling, reaching
   repeated-CV MSE 1362.697 (RMSE 36.915), with importance reported
   as normalized total gain.
   For classification, your write-up reports a 2,000-tree random
   forest at depth 5 with mtry=6 and log-loss splits, using
   fold-local random oversampling to a 0.20 minority/majority ratio
   as the declared imbalance method, with mean log loss 0.370461.
   The XGBoost classifier is described with 122 trees,
   learning_rate=0.03, depth 3, scale_pos_weight=1.5, and an
   eight-fold OOF sigmoid calibration, with mean log loss 0.381670.
   You also state that resampling, early stopping, and calibration
   were restricted to training folds and that classification models
   were selected by probability cross-entropy rather than threshold
   accuracy.
   Suggested further reading
   Breiman, L. (2001). Random forests. Machine Learning, 45(1),
   5-32. https://doi.org/10.1023/A:1010933404324
   Chen, T., & Guestrin, C. (2016). XGBoost: A scalable tree
   boosting system. In Proceedings of the 22nd ACM SIGKDD
   International Conference on Knowledge Discovery and Data
   Mining (pp. 785-794).
   https://doi.org/10.1145/2939672.2939785
   Hastie, T., Tibshirani, R., & Friedman, J. (2009). The
   elements of statistical learning (2nd ed., Chapters 9, 10,
   15). Springer.
   https://doi.org/10.1007/978-0-387-84858-7
   Niculescu-Mizil, A., & Caruana, R. (2005). Predicting good
   probabilities with supervised learning. In Proceedings of
   the 22nd International Conference on Machine Learning
   (pp. 625-632). https://doi.org/10.1145/1102351.1102430

4. FINAL GRADE
----------------------------------------------------------------------

   Formula: Final = 0.70 * Performance + 0.20 * Slides + 0.10 * Code

   Performance:  100.0/100  x 0.70 = 70.0
   Slides:       100/100  x 0.20 = 20.0
   Code:         100/100  x 0.10 = 10.0
   ──────────────────────────────────────────
   FINAL SCORE:  100.0/100

======================================================================
  END OF REPORT
======================================================================